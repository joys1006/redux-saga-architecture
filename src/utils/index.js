// filters -> date,num,validation
import * as filters from './filters';
// mixins -> dom
import * as mixins from './mixins';
//storages -> cookies,local,session -> get,set
import * as storages from './storages';

export {
    filters,
    mixins,
    storages,
};